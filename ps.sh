# ps # shows the processes of the current shell showing process id, tty (terminal type), cpu time, cmd
# ps -e # shows all running processes
# ps aux # shows all running processes in BSD format
# ps ax # shows all running processes
# ps -ef # shows all running processes in full format listing
# ps -u root # get all processes of root
# ps -ef | grep 123
