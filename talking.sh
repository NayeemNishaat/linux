# WALL
# wall # after running the command write the message and go to a new line then to send the message hit control+d

# USERS
# users # get active users

# WRITE
# write laby # will send message to laby only (control+d to stop sending)
